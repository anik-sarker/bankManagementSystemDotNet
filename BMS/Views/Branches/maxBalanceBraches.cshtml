@model IEnumerable<BMS.Models.Branch>

@{
    ViewBag.Title = "maxBalanceBraches";
    Layout = "~/Views/Md/mdView.cshtml";
}

<h2>maxBalanceBraches</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurrentEmployees)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurrentManagers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BranchBalance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentEmployees)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentManagers)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BranchBalance)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
